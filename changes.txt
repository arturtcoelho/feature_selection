better title

on Table 4.1 we have something really interesting, that we need to point out:
ridge is always better with k=1, the same is not true with the other models, going as as preffering k=0.5 in some cases, both for extratrees as for xgboost
we need to point out specifically that when k=1, there is no feature selection winner man
this 1st graph is already awesome for telling that: "if k=1, we know that baseline is winning, that is usually the case anyway, but in some cases, we have better results with lower ks, and they respective strategies" -> in the paper, we will manually write on top of MI -> "baseline", its not MI, its the full ffeature set

we will need some way to show the winner at every k value, this is done somewhat with the bar graphs

figure 4.1 is too small as well
same on fig 4.2

scrap table 4.2, the figure shows better results, specially the fact that shap can be different on each dataset

for 4.2, we can say that the datasets picked were better cause we can see an actual difference in performance at the k levels, as well as a bigger number of features to use a finer grain of k values

the time comparison table for exp 2 is gone...
the time for shap and hybrid here is completelly off, hybrid EXPERIMENT takes a lot longer cause we cant reuse the removed features, so hybrid MUST be faster than base shap, understand the implementation and recalculate, also, by the very nature of how its implemented, thers no way that higher k values take longer for both hybrid as custom shaprfe

-> we need to, at some point, id say either at 3 or 4, talk more about the specific implementations used, both in general, as well as the custom algorithms